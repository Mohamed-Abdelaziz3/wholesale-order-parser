# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
jinja2>=3.1.0
python-multipart>=0.0.9
aiofiles>=24.1.0

# Sessions / signed cookies (starlette SessionMiddleware)
itsdangerous>=2.2.0

# Matching
rapidfuzz>=3.9.0

# LLM extraction.
# NOTE: the import is `from google import genai`, which is provided by the
# `google-genai` package. The older `google-generativeai` package provides
# `google.generativeai` and will NOT satisfy this import.
google-genai>=1.0.0

# Config
python-dotenv>=1.0.0

# Catalog upload: .xlsx support (merchants send Excel files)
openpyxl>=3.1.0

# Tests
# numpy is imported by evaluation/reranker_benchmark/score_results.py; without it
# a plain `pytest` aborts during collection before running a single test.
numpy>=1.26.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx2>=2.7.0
