# v3.0.0 — من نموذج أولي إلى منتج قابل للعرض

> كل عيب في القائمة دي كان **مُعاد إنتاجه عملياً** قبل الإصلاح، و**مُعاد اختباره عملياً** بعده.
> الاختبارات: **211 نجحوا** (كانوا 148). الـ 3 الفاشلين بيعتمدوا على ملفات في `evaluation/`
> مش موجودة في نسخة العمل عندي — هينجحوا على جهازك.

---

## 🔴 موانع كانت هتمنع أي عرض

| # | كان | بقى |
|---|---|---|
| 1 | `requirements.txt` فيه `google-generativeai` والكود بيستورد `google-genai` → **التطبيق مش بيقوم من تثبيت نظيف** | الباكدج الصح + تعليق يشرح الفرق + خطوة CI بتتأكد إن `import app.main` بينجح من تثبيت نظيف |
| 2 | **صفر مصادقة** — أي حد يوصل للرابط يقرا ويعدّل ويعتمد أي طلب | مصادقة إجبارية على كل endpoint للبيانات. `GET /api/orders` بدون دخول = **401** |
| 3 | حقل `actor` مجرد string من العميل → منظومة الـ audit كلها ديكور | الهوية من **الجلسة**. طلب باسم مختلف = **403** (مش تصحيح صامت) |
| 4 | **Stored XSS** — نص عميل التاجر بيتحقن خام في `innerHTML` | كل قيمة غير موثوقة بتعدي على `esc()` قبل ما توصل للـ DOM |
| 5 | **CSV formula injection** في ملف مُحسَّن أصلاً لـ Excel | أي خلية بتبدأ `= + - @` بتتسبق بـ `'` — 0 خلايا تُنفَّذ |

---

## ⚡ الأداء وقاعدة البيانات

| البند | قبل | بعد |
|---|---|---|
| 5 طلبات متوازية (استدعاء مزوّد 1 ثانية) | **5.17 ث** (متسلسل، توازي = 1) | **1.10 ث** — endpoints بقت `def` فبتشتغل في thread pool |
| 200 قراءة من قاعدة البيانات | **77 fd مفتوح** | **4 fd** — `session()` بتقفل الاتصال في `finally` |
| قراءة الحالة ثم الكتابة (اعتماد/مراجعة/تصدير) | TOCTOU | `BEGIN IMMEDIATE` |
| فهارس | على `audit_events` بس | + `order_items`, `orders`, `catalog_products` |

## 🧠 سلامة البيانات

* **جدول `corrections` كان مسموم**: قبول ترشيح النموذج بدون أي تعديل كان بيكتب تصحيح كاذب
  `product_id: None → SKU`. يعني **100% من بيانات "التعلّم من التصحيحات" كانت نويز**.
  دلوقتي خط الأساس هو ترشيح النموذج، فصفر تصحيحات كاذبة — واختبار بيحرس ده.
* **مقياس الوقت بقى صادق**: قبل الاعتماد بيعرض زمن المعالجة الآلية بس ويقول صراحة إن
  الوقت الموفَّر لسه ما اتحققش. بعد الاعتماد بيعرض **الدورة الكاملة المقيسة** مقابل خط أساس
  يدوي مُعلَّم بوضوح كتقدير.

## 🔌 صلابة المزوّد

* timeout على كل استدعاء + إعادة محاولة بـ exponential backoff و jitter على 429/503/timeout.
* تفرقة بين خطأ مؤقت (إعادة على نفس الموديل) وخطأ دائم (انتقل للموديل التالي) —
  ده بيعالج مباشرةً الـ **20% API error rate** اللي ظهر في التقييم.
* فشل المزوّد بيرجّع **503** مش 500، ورسائل الأخطاء الداخلية مبقتش تتسرّب للعميل.

---

## 🆕 مميزات جديدة (اللي كانت ناقصة للديمو)

### 1. رفع كتالوج التاجر — **بدونها مكانش فيه ديمو أصلاً**
- CSV و **XLSX**
- ترميزات: UTF-8، UTF-8-BOM، و**cp1256** (اللي بيطلع من Excel العربي على ويندوز)
- فواصل: `,` `;` tab
- **عناوين أعمدة بالعربي أو بالإنجليزي**: `كود المنتج`/`sku`، `اسم المنتج`/`الصنف`، `مرادفات`، `الوحدة`، `السعر`
- أعمدة ناقصة → تحذير واضح مش رفض
- أكواد مكررة/ناقصة → رفض برقم الصف
- الاستبدال ذرّي، **والطلبات المعتمدة القديمة بتفضل زي ما هي**

### 2. تسجيل دخول + هوية مراجع حقيقية
صفحة دخول، جلسة موقّعة، خروج، throttle ضد التخمين، وكل قرار متسجّل باسم الحساب الفعلي.

### 3. سجل الطلبات
`GET /api/orders` + لوحة في الواجهة — تفتح أي طلب قديم وتكمل مراجعته.

### 4. اختيار مسبق للترشيح ← **أعلى ROI في الجلسة كلها**
قبل: 5 أصناف بثقة 100% و**كل الـ dropdowns فاضية**. التاجر بيدور في الكتالوج كله 5 مرات.
بعد: الترشيح مختار مسبقاً + زرار **«تأكيد السطور الأكيدة (N)»**.

### 5. معالجة الغموض — إصلاح اكتشفته في محاكاة ديمو حقيقية
لقيت إن `رز الضحى 5 كيلو` بيطابق **100%** ومع ذلك الواجهة بتقول «مفيش ترشيح»، لأن
مقاس تاني قريب (1 كيلو) جاب سكور قريب فالنظام كان **بيخفي الإجابة الصح**.

- السطر الغامض دلوقتي **بيعرض أفضل مرشّح** مع تحذير `⚠️ فيه أصناف تانية قريبة جداً — اتأكد من الحجم/العبوة`
- التأكيد الجماعي بيشتغل **بس على السطور الأكيدة** (`SELECT`) — مقاس قريب عمره ما يتختم بالجملة
- النتيجة في الديمو: **4 من 5 سطور مملوءة صح** بدل 2

---

## 🚢 النشر
`Dockerfile` (مستخدم غير root + healthcheck) · `Procfile` · `railway.json` · `fly.toml` ·
`pyproject.toml` · **CI على GitHub Actions** بتتأكد إن التطبيق بيقوم من تثبيت نظيف ·
`DEPLOY.md` بخطوات Railway/Fly/Docker + تشيك ليست قبل أي عرض · `ORDERS_DB_PATH` لـ volume دائم.

## 🧪 الاختبارات
`tests/test_security_and_catalog.py` — **63 اختبار جديد**، كل واحد منهم مربوط بعيب حقيقي:
المصادقة على كل endpoint، تزوير الـ actor، حقن CSV، الـ escaping، رفع الكتالوج بكل الترميزات
والعناوين، تسميم جدول corrections، إغلاق الاتصالات، صدق مقياس الوقت، تصنيف أخطاء المزوّد،
ومعالجة الغموض.

---

## ⚠️ حدود لسه موجودة — قولها للعميل بصراحة

| الحد | الوضع |
|---|---|
| **مستأجر واحد** | نسخة = تاجر واحد. تاني عميل = نسخة تانية. (مقصود — مش وقته دلوقتي) |
| SQLite | ممتازة لعشرات الآلاف من الطلبات على نسخة واحدة |
| الهوية | باسورد مشترك أو حسابات `name:password`. مفيش SSO/2FA |
| مفيش ربط واتساب | لسه نسخ ولصق. **متعملهاش قبل ما عميل يدفع** |
| حصة Gemini المجانية | 429 تحت الضغط. النظام بيعمل retry، بس الحمل الحقيقي عايز حساب مدفوع |
| خط الأساس اليدوي (45 ث/صنف) | **افتراض مش قياس** — عايره على شغل التاجر من أول أسبوع |
